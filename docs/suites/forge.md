# Forge Suite

## Purpose

Forge is a user-composed suite. It does not introduce unique services; it selects from Boosting, Neural, and Flow components and writes a custom Compose file.

## Behavior

- The CLI presents a checklist of candidate services.
- Port conflicts are resolved before the file is written.
- Only selected services are enabled in the rendered Compose output.
